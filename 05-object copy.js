let user = {}
user.id = 10
user.name = 'Alice'

console.log(user.id + 400)

// console.log('user' + user.id) > must use ' ' outside user
console.log('user' + user.id + ' is ' + user.name)// must use ' ' outside user